# 06 · 各文档待修订项清单

> **本文件性质**：`00`~`05` 的一致性核对记录与修订工作单。**不是**设计正文，不参与 `00` §3.1 的表数口径。
> **核对基准**：`00` §3.1 统计口径（唯一权威） + `server/prisma/schema.prisma`（旧库 33 Model） + `server/prisma/migrations/**`（物理表/索引/约束的真实名字）。
> **状态图例**：`✅ 已修订`（本轮已改完） · `⬜ 待修订` · `➖ 无需修订`。

---

## 1. 口径闭合校验（核对基准，先确认再谈修订）

### 1.1 目标表数闭合

| 层 | 目标业务表 | 沿用 | 改造 | 扩展 | 重命名 | 重构 | 拆分(源) | 合并 | 删除 | 拆分派生(新) | 无来源(新) |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `01` 公共支撑 | 13 | 5 | 5 | — | — | — | — | — | — | — | 3 |
| `02` 主数据 | 13 | 8 | 1 | 1 | — | 1 | — | — | 1 | — | 2 |
| `03` 销售 | 11 | 1 | 2 | — | 2 | — | 1 | 1 | — | 2 | 3 |
| `04` 履约 | 13 | — | — | 1 | 2 | — | 1 | — | — | 8 | 2 |
| **合计** | **50** | **14** | **8** | **2** | **4** | **1** | **2** | **1** | **1** | **10** | **10** |

- 旧 Model 总数 = `14 + 8 + 2 + 4 + 1 + 2 + 1 + 1` = **33** ✅ 与 `schema.prisma` 一致。
- 目标业务表 = `13 + 13 + 11 + 13` = **50** ✅。
- 新增 20 = 拆分派生 10 + 无来源 10 ✅。
- 「拆分(源)」与「合并(源)」列**不产出目标表计数**（`SalesPipeline` 的产出已计入改造/拆分派生/无来源；`Order(type=QUOTE)` 的产出计入 `Quotation` 的「合并」）。

### 1.2 物理表名 / 索引名核对（`02` §3.1 专用）

| 对象 | 物理名字 | 来源 |
|---|---|---|
| 关联表（当前） | `_ProductCraftToSingleProduct`（列 `A` → `SingleProduct.id`、`B` → `ProductCraft.id`） | `migrations/20260824000000_split_single_combo` 由 `_ProductToProductCraft` 改名 |
| 关联表唯一索引 | **仍是** `_ProductToProductCraft_AB_unique` | `migrations/20260820023856_init:400`（改名时未同步索引） |
| 关联表 B 索引 | **仍是** `_ProductToProductCraft_B_index` | `migrations/20260820023856_init:403` |

> 结论：`ProductCraftLink` **不是「零 DDL」**，见 §3 第 4 项。

---

## 2. `00-总览与设计原则.md`

| # | 位置 | 核对结论 | 状态 |
|---|---|---|---|
| 1 | §3.1 统计口径（L128~L140） | 旧 33 / 新 50 / 新增 20 / 处置 `沿用14·改造8·扩展2·重命名4·重构1·拆分2·合并1·删除1` —— 与 §1.1 闭合校验**完全一致** | ➖ |
| 2 | §3.1 关联表说明（L139） | `_ProductCraftToSingleProduct` 表名正确；「物理表总数与旧库一致」表述正确（只差索引名，属 `02`/`05` 的细节） | ➖ |
| 3 | §3.5 拆分后消失的旧 Model（L212~L215）| `SalesPipeline` / `Order` / `ProductActivity` 口径正确 | ➖ |
| 4 | §5 ADR-01 / ADR-02（L291~L292）| 引用 `05` §3 / `05` §4，章节号成立 | ➖ |
| 5 | §5 ADR-15（L305）、ADR-17 | 引用 `05` §10、`02` §3.1，章节号成立 | ➖ |
| 6 | §7.1 R5（L472）、§8 红线（L479~L484） | 引用 `05` §5 / `05` §13，章节号成立 | ➖ |

> **`00` 无需修订**：它是口径源，本次核对未发现自相矛盾或与旧库不符之处。

---

## 3. `01-公共支撑层.md`

| # | 位置 | 问题 | 修订内容 | 状态 |
|---|---|---|---|---|
| 1 | §5.1 `NumberSequence.code` 注释 | code 清单只有 14 个（`LEAD / OPP / QUO / SMP / SO / PO / PR / SHP / INS / PAY / PRF / CUS / PRD / SUP`），**缺 `CMB`**；而 `02` §5 `ComboProduct.comboNo // CMB-yyyyMMdd-0001` 要求发 `CMB` 号 | 注释末尾补 ` / CMB` | ✅ |
| 2 | §5.1「编号规则登记」块 | 同上，缺 `CMB` 的格式说明行 | 补 `/// CMB   → CMB-yyyyMMdd-0001 组合产品编号` | ✅ |
| 3 | §7 表数小结（L480） | 13 = 无来源 3 + 改造 5 + 沿用 5 ✅ 数学闭合 | — | ➖ |
| 4 | §7 #6（L475） | 指向 `05` §5 补号 + `currentValue` 同步，与 `05` §5 的 15 个 code 保持一致（补 `CMB` 后） | — | ➖ |

> **连带影响**：`05` §5 的补号清单必须从 14 个 code 扩为 **15 个**（含 `CMB`）。

---

## 4. `02-主数据层.md`

| # | 位置 | 问题 | 修订内容 | 状态 |
|---|---|---|---|---|
| 1 | §2.2 `Product.packaging` 列注释（L82） | 写「（新增，无旧字段来源）」，但旧 `SingleProduct` **存在** `packaging String? // 产品包装（卡/盒/袋/桶）` | 改为「（保留：旧 `SingleProduct.packaging`）」 | ✅ |
| 2 | §2.1 字段去向表（L165） | `Product.moq / leadTime / hsCode / **packaging**` 被合并成一行标注「新增，无旧字段来源（旧库无对应列）」—— `packaging` 判断错误 | 拆成两行：`moq/leadTime/hsCode` 为新增；`packaging` 为**保留** | ✅ |
| 3 | §11 #7（L535） | 把 `packaging` 列入「新增，无旧字段来源」 | 从新增列表移除，并显式说明 `packaging` 属保留 | ✅ |
| 4 | §3.1 `ProductCraftLink` 迁移动作（L267） | 断言「**本项不需要任何 DDL**」—— 实测 2 个索引名仍是 `_ProductToProductCraft_*`，Prisma 会按 `@@map` 推导出 `_ProductCraftToSingleProduct_*`，故 `migrate diff` **一定产生 DDL** | 改为「表/列零 DDL + 2 条索引改名必须人工写」并给出 `ALTER INDEX ... RENAME TO ...` | ✅ |
| 5 | §11 #4（L531） | 与第 4 项同源的「不产生 DDL」表述 | 改为「表/列零 DDL，但索引名不一致会产生 2 条 DDL，禁止 DROP + CREATE」 | ✅ |
| 6 | §3.1 表数口径（L521） | `沿用/改造 12 + 无来源新增 2 − 删除 1 = 13` ✅ 与 §1.1 闭合 | — | ➖ |
| 7 | §2.1 `sizeL/sizeW/sizeH/weight` → `Float?`（L160） | 与 `04` §12 #6「`Float` 仅用于 `grossWeight/netWeight/volume`」不冲突（后者是 `04` 本层口径），无需改 | — | ➖ |

> 复核确认旧 `SingleProduct` **确实有** `packaging`、**确实没有** `material` / `moq` / `leadTime` / `hsCode` / `productNo` / `status` / `deletedAt`，故 §2 的第 1~3 项是唯一错处。

---

## 5. `03-销售层.md`

| # | 位置 | 问题 | 修订内容 | 状态 |
|---|---|---|---|---|
| 1 | §2.2 `Customer` 反向关系 | 缺 `shipments Shipment[]`，而 `04` 的 `Shipment.customerId` 是**必填** `Restrict` → Prisma 校验会报「反向关系缺失」 | 已补 `shipments Shipment[]` | ✅（上一轮） |
| 2 | §2.2 `CustomerActivity` | 标注「沿用」但文档**没有模型体**，无法落地 | 已补完整模型 + 零 DDL 说明 | ✅（上一轮） |
| 3 | §2.1 字段去向表（L157~L168） | 表内未体现 `shipments` 这一新增反向关系（不影响可落地性，仅文档完整性） | 建议补一行 | ⬜ 低优先 |
| 4 | §3.2 `Lead.pipelineId`（L297） | 表述「**删除**（FK 迁移至 `Opportunity.leadId`）」+「`05` §3.3 有完整步骤」—— 与 `05` §3.3 需完全对齐 | 由 `05` §3.3 承接 | ➖ |
| 5 | §10 #1 / #2（L748~L749） | 「不得出现落库的 `Opportunity.stage`」「不得出现 `Lead.opportunityId`」与 `00` ADR-03、`05` §3.4 一致 | — | ➖ |
| 6 | §10 #6 / #7（L753~L754） | `probability` 18,6→实为 `Decimal(5,2)`（§10 #6 写的是 `probability` 5,2 ✅）；「旧 `SalesPipeline.probability` 中文文案迁至 `intentLevel`，**不得**写入新 `probability`」—— 与 `05` §3.2 一致 | — | ➖ |
| 7 | §10 #8 / #9（L755~L756） | `Customer.customerNo` / `Lead.leadNo` 补号，指向 `05` §5 ✅ | — | ➖ |

---

## 6. `04-履约层.md`

| # | 位置 | 问题 | 修订内容 | 状态 |
|---|---|---|---|---|
| 1 | §12 #11 | 写「`ProductionOrderItem.status` / `PurchaseOrderItem.status` / `PurchaseOrder.purchaseType` 已由 `String` 改枚举（Q1），迁移须走两阶段」—— 旧库**没有**这两张明细表，旧 `PurchaseOrder` 也**没有** `purchaseType` 列（旧列仅 `purchaseNo / supplierId / purchaseDate / status / items / amountCNY / remark / ownerId / createdBy`）。这三者属**新建**，不存在 `String → enum` 迁移 | 改为指明真正需要两阶段的列是 `PurchaseOrder.status`（旧 `String @default("DRAFT")`：`DRAFT`/`ORDERED`/`PARTIAL`），并显式声明另三者为新建、不需要两阶段 | ✅ |
| 2 | §2 Q10 说明（L245） | 「迁移时**不得**保留旧的 `SampleOrder.salesOrderId` 列（`05` §4.4 有 ALTER 步骤）」—— 与 `05` §4.4 一致 | 由 `05` §4.4 承接 | ➖ |
| 3 | §7 支付对照表（L855） | 「编号 `paymentNo String?` → `paymentNo String @unique`，补号 + 唯一（见 `05` §12）」—— 与 `05` §12 一致 | 由 `05` §12 承接 | ➖ |
| 4 | §12 #12（L991） | 同第 2 项 | 由 `05` §4.4 承接 | ➖ |
| 5 | §12 #6、#7 | `Float` 白名单与数量口径自洽 | — | ➖ |
| 6 | §9.1 成本归集公式 | 依赖 `PurchaseOrder.purchaseType` 三分类；该列为**新建列**，迁移时须给默认值（`05` §4.6 承接） | 由 `05` §4.6 承接 | ➖ |

---

## 7. `05-新旧模型映射与迁移.md`

`00`~`04` 对 `05` 的**章节号硬引用**（`05` 必须存在这些章节）：

| 引用方 | 章节号 |
|---|---|
| `00` | §1、§3、§4、§5、§10、§13 |
| `01` | §5、§10.1、§10.5 |
| `02` | §2.2、§5、§13 |
| `03` | §3.3、§5 |
| `04` | §4.4、§10、§12 |

因此 `05` 必须具备：`§1` `§2.2` `§3` `§3.3` `§4` `§4.4` `§5` `§10` `§10.1` `§10.5` `§12` `§13`。

本轮 `05` 待修订项（**由重写一次性解决**）：

| # | 位置 | 问题 | 状态 |
|---|---|---|---|
| 1 | §1 统计行 | **自相矛盾且与 `00` §3.1 不符**：写「沿用 14 · 改造 7 · 扩展 4 · 拆分 2 · 合并 1 · 重命名 2 · 删除 1 · 新增无来源 5（计 7 张）」，既多出 `00` 已明令废弃的口径「新增无来源 5」，又与 §1.1 闭合校验冲突（应为 `沿用14·改造8·扩展2·重命名4·重构1·拆分2·合并1·删除1 = 33`） | ✅ 已修订（新 §1.1/§1.2 逐字对齐 `00` §3.1，并显式声明禁用废弃口径） |
| 2 | §1 逐项表 | `PaymentRecord` / `ProfitRecord` 标「扩展」应为**重命名**；`ApprovalConfig` / `DailyExchangeRate` 标「沿用」应为**改造**；`Supplier` 标「沿用」应为**扩展**；缺 `SingleProduct` 的**重构**与 `Quotation` 的**合并**；`User` / `Department` / `OperationLog` / `Customer` / `ComboItem` / `Lead` 的处置未与 `00` 对齐；`ProductPrice` / `CustomerProduct` 归层与 `03` 不符 | ✅ 已修订（新 §1.3 共 33 行；`ProductPrice`/`CustomerProduct` 明确为 `03` 层无来源新增） |
| 3 | §1 同名 `pipelineId` 行 | 写成「`pipelineId` → `opportunityId`」，与 `03` §10 #2「**不得**出现 `Lead.opportunityId`」直接冲突（正确做法：FK 搬到 `Opportunity.leadId`） | ✅ 已修订（§1.4 红线 1 + §3.3 完整搬迁步骤） |
| 4 | §2.2 产品迁移 | `crafts` 关联表名写成 `_ProductToProductCraft`（实际应为 `_ProductCraftToSingleProduct`）；`features` 写成 `FeatureFlags`（应为 `Json?` + Zod）；`price/currency/taxRate` 写成「直接去 `defaultPrice`」（实际是**列改名 + `Float → Decimal` + `String → Currency`**）；`packaging` 误列为「新增无旧字段来源」；漏 `Product.status`（重新引入）与 `deletedAt`（新增） | ✅ 已修订（§2.2 字段去向表 + DDL，表名改 `_ProductCraftToSingleProduct`，`features Json?`，三列改名+类型，`packaging` 保留，`status`/`deletedAt` 补齐） |
| 5 | §3.2 商机迁移 | `currency <- 'CNY'` 未同步 `exchangeRate <- 1`；`probability` 映射到新 `probability`（应为旧中文文案 → `intentLevel`，新 `probability` 置 NULL）；漏 `outcome` / `outcomeAt` / `wonAt` / `lostReason` 物化；`stage` 处理未按 `00` ADR-03 写成「派生不落库」 | ✅ 已修订（§3.2 含 CNY⇒1 断言、中文→`intentLevel` 映射表、`stage` 不落库；§3.6 物化 SQL） |
| 6 | §3.3 | 需改写为「`Lead.pipelineId` → `Opportunity.leadId` FK 搬迁回填」（含 `DROP COLUMN` 前置约束与 `LeadItem` 迁移顺序） | ✅ 已修订（§3.3 三条前置约束 + Step A~D；§3.5 `LeadItem` 重映射须先于 `DROP COLUMN`） |
| 7 | §4.2 | `o."orderNo"` 直接当 `quotationNo` —— 旧列为 **nullable**，且 `Quotation.quotationNo` 为 `@unique` 必填；缺「新号生成 + 兜底 + 与旧 `Quotation` 表合并时 `@@unique([opportunityId, version])` 去重 + `version` 重排」 | ✅ 已修订（§4.2 含发号兜底去重、两套来源合并、`version` 重排、`customerId` 兜底、校验 SQL） |
| 8 | §4.4 | 缺 `SampleOrder.salesOrderId` 的 `ALTER TABLE ... DROP COLUMN` 步骤与 `SalesOrder.sampleOrderId` 反向回填 SQL | ✅ 已修订（§4.4 含唯一配对反填 SQL + `DROP COLUMN IF EXISTS "salesOrderId"` + 校验） |
| 9 | §5 补号 | 缺 `CMB`（`ComboProduct`）；`currentValue` 同步只给了一个示例，未覆盖全部 15 个 code | ✅ 已修订（§5 补号规则表含 `CMB` 共 15 个 code，并给出 15 行 `currentValue` 同步清单） |
| 10 | §10 / §11 / §12 / §13 | **整章缺失**（`01`/`04`/`00` 正在引用）。需补：`String → enum` 两阶段、`ApprovalConfig` 列改名+类型+值重命名、`DailyExchangeRate` 反向语义换算、`Payment.paymentNo` / `Profit.profitNo` 补号、rename / join table 红线 | ✅ 已修订（新增 `§10` 两阶段枚举迁移、`§11` JSON/附件/多态日志、`§12` 收付款/利润补号与成本派生、`§13` rename/join table 红线） |

> **顺序说明**：先完成 §3~§6（`01`/`02`/`04` 的小修），再重写 `05`（§7），避免 `05` 引用到错误口径。
> **本轮结果**：`05` 已按上表 10 项**一次性重写**（全文 13 章 + 附变更记录），硬引用章节 `§1` `§2.2` `§3` `§3.3` `§4` `§4.4` `§5` `§10` `§10.1` `§10.5` `§12` `§13` **全部存在**。重写过程中新增了 3 个阻断项（`B6` 收付款/利润宿主无法映射、`B7` `ShipmentItem.salesOrderItemId` 无法匹配）与 2 条口径修正（Quotation/SalesOrder 原币金额不可还原、`Lead.status` 的 `VALID → QUALIFIED`），详见 `05` §7.1 / §10.2。

---

## 8. 核对方法（可复现）

```bash
# 1) 目标表清单（每层 model 数）
cd docs/V1.0目标Schema设计
for f in 01-公共支撑层.md 02-主数据层.md 03-销售层.md 04-履约层.md; do
  echo "$f: $(grep -cE '^model ' $f)"
  grep -E '^model ' $f | sed 's/^model //;s/ {.*//' | tr '\n' ' '
  echo
done

# 2) 旧库旧模型数与关键列
cd ../../server/prisma
grep -cE '^model ' schema.prisma
sed -n '/^model SingleProduct {/,/^}/p' schema.prisma | grep -E 'packaging|material|moq|leadTime|hsCode|productNo'

# 3) 关联表与索引的真实物理名
grep -rn "_ProductCraftToSingleProduct\|_ProductToProductCraft" migrations/

# 4) 00~04 对 05 的章节引用（05 章节结构的硬约束）
cd ../../docs/V1.0目标Schema设计
grep -noE '`05`[^，。；)）]*' 0[0-4]-*.md
```

---

> **本文件仅记录核对与修订项，不改变 `00` §3.1 的任何口径。**
> 状态为 `✅` 的条目已在本轮落地；`⬜` 为遗留的低优先项。
