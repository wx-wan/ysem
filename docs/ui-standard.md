# UI 标准（基线规范 v1 · 线索页）

> 来源：参考图（新建询盘向导 / 列表卡片+详情 / 公海 / 筛选分段）+ 已落地实现。
> 范围：先统一「线索页」全部 UI，作为全站基线；其余页面（客户/订单/采购/仪表盘）后续按本规范逐页推广。
> 说明：当前模型无法读取图片，下列数值基于已落地实现与参考图还原；带 ⚠️ 的项建议你核对后确认/微调。

---

## 1. 设计原则

1. **单一品牌色（蓝）**：全站以 `#1677ff` 为唯一主色（与 antd `colorPrimary` 一致）。所有主操作、选中态、强调元素统一用蓝色系，禁用平行色板。
2. **圆角语言收敛**：卡片 16、内部块 12、小元素 8、胶囊 6（全站统一）。
3. **轻投影 + 描边**：卡片靠「白底 + 1px 描边 + hover 轻投影」分层，不依赖重阴影。
4. **状态用语义色**：成功/警告/危险/信息用绿/橙/红/蓝，不碰品牌蓝（信息=蓝可复用主色）。
5. **字体统一**：`Montserrat` + `SourceHanSansCN`，已全局 `!important` 覆盖 antd。

---

## 2. 色彩 Token

| 名称 | HEX | 用途 | 全局变量 |
|---|---|---|---|
| 品牌渐变 | `#4096ff → #1677ff` | 向导/详情头部、CTA 背景 | — |
| 主色实心 | `#1677ff` | 主按钮、胶囊选中、激活描边、步骤强调 | `--c-primary`（已 `#1677ff`） |
| 主色悬停 | `#4096ff` | hover 态 | `--c-primary-hover`（已 `#4096ff`） |
| 主色加深 | `#0958d9` | active/pressed | `--c-primary-active` |
| 主色光晕 | `rgba(22,119,255,0.15)` | 选中卡片外圈 ring | `--c-primary-ring` |
| 主色浅底 | `#e6f4ff` | 选中卡片/块底色、蓝标签浅底 | `--c-primary-bg`（已 `rgba(22,119,255,.08)`） |
| 主色描边浅 | `#91caff` | hover 描边 | `--c-primary-border` |
| 主色文字 | `#1677ff` | 线索号、查看详情等文字链接色 | — |
| 文本主 | `#1e293b` | 标题/正文 | `--c-text` |
| 文本次 | `#64748b` | 副信息 | `--c-text-secondary` |
| 文本弱 | `#94a3b8` | 占位/弱提示 | `--c-text-tertiary` |
| 面（白） | `#ffffff` | 卡片/面板底色 | `--c-surface` |
| 背景 | `#f8fafc` | 页面底色 | `--c-bg` |
| 描边-强 | `#e2e8f0` | 卡片/面板边框 | `--c-border-strong` |
| 描边-弱 | `#f1f5f9` | 内部分隔 | `--c-border` |

> 语义状态色：成功 `#10b981` / 警告 `#f59e0b` / 危险 `#ef4444` / 信息 `#1677ff`。

---

## 3. 形状 / 间距 / 阴影 / 字体

| 类别 | 值 | 说明 |
|---|---|---|
| 圆角-卡片 | 16 | 列表卡片、详情面板、外层面板 |
| 圆角-中 | 12 | 内部块（summary、胶囊容器、输入组） |
| 圆角-小 | 8 | 标签内联、小按钮 |
| 圆角-胶囊 | 6(pill) / 20(按钮) | 胶囊分段、圆角按钮 |
| 间距 | 卡片内 16–18，列表 gap 12，块间距 12 | 统一留白 |
| 阴影-常驻 | `0 1px 3px rgba(0,0,0,.04)` | 面板默认 |
| 阴影-hover | `0 8px 24px rgba(0,0,0,.08)` | 卡片 hover / 浮层（已统一） |
| 字体 | Montserrat + SourceHanSansCN | 标题 700、强调 600、正文 400/500 |
| 字号 | 标题 20 / 区块标题 15 / 正文 13 / 弱 12 | 线索页已用此档 |

---

## 4. 组件规范（线索页）

### 4.1 新建入口 CTA（`LeadCreateCard`）
- 整条**蓝色渐变**横条，白字；左侧 48 圆角方块白底蓝字「+」；右侧白色圆形箭头。
- hover：整体上浮 2px + 阴影增强；支持键盘 Enter/Space。

### 4.2 范围切换 / 分段筛选（`CapsuleSwitch`，线索页用法）
- 容器：白底 + 1px 描边 + 圆角 12 + 轻投影。
- 选中项：蓝色 `#1677ff` 实心胶囊，白字 + 主色投影；未选中：灰字 `#64748b`。
- 尺寸：padding `4px`，项 padding `5px 14px`，字号 13。
- 用法：我的线索 / 公海 切换；参考图「全部/待处理/跟进中/已报价」同款。

### 4.3 卡片列表（`LeadCardList` / `LeadCard`）
- 卡片：白底，1px 描边 `#e2e8f0`，圆角 16，padding `16px 18px`，gap 12。
- hover：描边 `#91caff` + 阴影 `0 8px 24px rgba(0,0,0,.08)`。
- 选中（`.is-active`）：描边 `#1677ff` + `0 0 0 2px rgba(22,119,255,.15)` 外圈。
- 结构：左头像 → 中（线索号蓝字 + 状态/类型 Tag + 公司名+国旗 + 产品 + 数量/需求）→ 右（日期 + 负责人 + 「查看详情」蓝字）。
- 头像底色：`leadAvatarColor` 取色板 `['#1677ff','#0ea5e9','#10b981','#f59e0b','#ef4444','#6366f1']`（稳定取色，区分负责人）。

### 4.4 详情侧边面板（`LeadDetailPanel`）
- 容器：白底，圆角 16，描边，阴影 `0 8px 24px /.06`，`position: sticky`。
- 头部：**蓝色渐变**，白字：线索号+状态 Tag / 公司名+国旗 / 产品 / 数量+日期；右上圆形关闭键。
- 内容：`Tabs`（详细信息 / 跟进记录），信息以 `lead-wizard-summary` 块（浅底描边圆角 12）呈现键值对。
- 底部操作条：公海→整条「认领」蓝按钮；私海→「确认转商机」主按钮 + 编辑/释放/删除（danger）。

### 4.5 三步向导弹窗（`LeadFormModal`）
- 头部：**蓝色渐变**，含关闭键、标题（线索名/新建标题 + 线索号徽标）、副标题（第 X/3 步 · 步骤名）、步骤条。
- 步骤条：已完成=白底蓝字对勾；当前=放大 1.18× + 白底蓝字 + 外圈光晕，标签加粗放大带阴影；已完成段连接线高亮白。
- 表单：`size="large"`，控件 `variant="filled"`；自定义控件透传 `size`。
- 底部：左侧返回/取消，中间进度小圆点，右侧下一步/保存/认领/确认（均 `size="large"`，主按钮蓝）。
- 负责人卡片：选中=蓝描边 + 蓝浅底 `#e6f4ff` + 外圈 ring；hover=蓝浅描边。

### 4.6 按钮
- 主按钮：`type="primary"`（蓝），圆角（antd 默认 token 12），`size="large"` 用于向导/详情主操作。
- 次/文字按钮：白底描边或纯文字，灰字；危险用 `danger`。

### 4.7 标签 / 状态（`STATUS_META`）
- 状态用 antd `Tag` 的 `color`（语义色）；客户类型等用默认描边 Tag。

### 4.8 输入框 / 选择
- 全部 `size="large"`；表单内 `variant="filled"`（浅填充）。
- 自定义封装（CountrySelect/CustomerTypeSelect/ContactMethodInput）已支持 `size` 透传；统一 `variant="filled"`。

### 4.9 表格（`LeadTable`，旧版/兜底）
- 若保留表格视图：斑马/描边、状态 Tag、主色文字链接（查看）；后续整体迁到卡片列表风格。

---

## 5. 落地状态（本页）

| 模块 | 状态 |
|---|---|
| CTA 新建入口 | ✅ 蓝渐变 + 上浮 |
| 范围胶囊切换 | ✅ 白底描边 + 蓝实心 |
| 卡片列表/卡片 | ✅ 圆角 16 + 统一 hover 阴影 + 蓝选中 |
| 详情面板 | ✅ 蓝渐变头部 + summary 块 |
| 三步向导 | ✅ 蓝渐变头 + 强调步骤条 + 蓝选中 |
| 按钮/输入 | ✅ large + filled |
| 全局主色 | ✅ 已为蓝 `#1677ff`（与标准一致） |

---

## 6. 推广到其他页的检查清单（后续）

1. 页面外层 `Card` 圆角 16、描边、轻阴影统一。
2. 分段/筛选用 `CapsuleSwitch`（白底描边 + 蓝实心）。
3. 主按钮/链接/激活态统一蓝；状态用语义色。
4. 表单 `size="large"` + `variant="filled"`。
5. 复用 `.lead-wizard-summary`、`.lead-card*` 等已验证样式类，避免重造。
6. 全站主色已是蓝，无需改 token；仅需对齐组件风格。
